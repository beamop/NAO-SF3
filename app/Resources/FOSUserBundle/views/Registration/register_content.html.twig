{% block content %}
    <!-- Titlebar
    ================================================== -->
    <div id="titlebar" class="gradient">
        <div class="container">
            <div class="row">
                <div class="col-md-12">

                    <h2><i class="sl sl-icon-login"></i> Inscription</h2>

                    <!-- Breadcrumbs -->
                    <nav id="breadcrumbs">
                        <ul>
                            <li><a href="#">Accueil</a></li>
                            <li>Inscription</li>
                        </ul>
                    </nav>

                </div>
            </div>
        </div>
    </div>


    <!-- Content
    ================================================== -->

    <!-- Container -->
    <div class="container">

        <div class="row">
            <div class="col-lg-12">

                <div class="notification notice large margin-bottom-55">
                    <h4>Information 🙂</h4>
                    <p>N'hésitez pas à contacter notre équipe si vous êtes naturaliste.</p>
                </div>

                <div id="add-listing" class="separated-form">
                    {{ form_start(form, {'method': 'post', 'action': path('fos_user_registration_register'), 'attr': {'class': 'fos_user_registration_register'}}) }}

                        <!-- Section -->
                        <div class="add-listing-section">

                            <!-- Headline -->
                            <div class="add-listing-headline">
                                <h3><i class="sl sl-icon-doc"></i> Création d'un nouveau compte</h3>
                            </div>

                            <!-- Nom d'utilisateur -->
                            <div class="row with-forms">
                                <div class="col-md-12">
                                    <h5>Nom d'utilisateur</h5>
                                    {{ form_widget(form.username, { 'attr': {'class': 'search-field'} }) }}
                                </div>
                            </div>

                            <!-- E-mail -->
                            <div class="row with-forms">
                                <div class="col-md-12">
                                    <h5>E-mail</h5>
                                    {{ form_widget(form.email, { 'attr': {'class': 'search-field'} }) }}
                                </div>
                            </div>

                            <!-- Mot de passe -->
                            <div class="row with-forms">
                                <div class="col-md-12">
                                    <h5>Mot de passe</h5>
                                    {{ form_widget(form.plainPassword.first, { 'attr': {'class': 'search-field'} }) }}
                                </div>
                            </div>

                            <!-- Confirmation mot de passe -->
                            <div class="row with-forms">
                                <div class="col-md-12">
                                    <h5>Confirmation du mot de passe</h5>
                                    {{ form_widget(form.plainPassword.second, { 'attr': {'class': 'search-field'} }) }}
                                </div>
                            </div>
                        </div>
                        <!-- Section / End -->

                        <input type="submit" class="btn btn-primary" id="register-submit-btn" value="S'inscrire" style="margin: 25px 0 25px 0;"/>
                    {{ form_rest(form) }}
                    {{ form_end(form) }}
                </div>
            </div>

        </div>

    </div>
    <!-- Content / End -->
    <!-- Container / End -->
{% endblock %}